---
- name: access_nodes
  hosts: access
  gather_facts: false

  tasks:
  - name: config vlan
    arista.eos.eos_config:
      parents: vlan '{{ item.vlan }}'
      lines:
        - name '{{ item.vlan_name }}'
    loop: '{{ access_interfaces }}'